apiVersion: 1

providers:
  - name: CEX load tests
    orgId: 1
    folder: CEX
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
